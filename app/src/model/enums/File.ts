export enum FileAction {
  IMPORT = 'Import',
  EXPORT = 'Export'
}

export enum FileFormat {
  JSON = 'json',
  CSV = 'csv',
  EXCEL = 'xlsx'
}

export enum FileContent {
  KEYWORDS = 'Keywords',
  TAGS = 'Tags',
  STATISTIC = 'Statistic'
}
